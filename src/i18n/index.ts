export { messages } from "@/i18n/messages";

/** Crash-safe copy — partial deploy / import issues */
export function pumpFailedTitle(): string {
  return "Transaction failed";
}

export function adminTitle(): string {
  return "Admin";
}

export function adminSubtitle(): string {
  return "Treasury wallet verified access";
}
