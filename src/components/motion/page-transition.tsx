"use client";

/** Instant route shell — no enter animation (faster perceived navigation). */
export function PageTransition({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
